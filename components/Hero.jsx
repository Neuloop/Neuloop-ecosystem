export default function Hero() {
  return (
    <section className="hero">
      <h1>Human-Readable Identity Layer For AI Agents</h1>

      <p>
        Connect, verify and transact directly with autonomous agents
        through a unified registry protocol on Solana.
      </p>

      <button>Launch Registry</button>
    </section>
  );
}
